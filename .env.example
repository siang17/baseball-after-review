# PostgreSQL (Supabase, Neon, Railway, Docker, or a self-hosted instance)
DATABASE_URL="postgresql://bar_user:change_me@localhost:5432/bar?schema=public"

# Required for non-development roster-import requests.
BAR_IMPORT_TOKEN="replace-with-a-long-random-secret"
